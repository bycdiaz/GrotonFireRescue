extends layout

include mixins/_editForm.pug

block content
  .editForms
    -let i = 0;
    while i < 2
      .formSection
        +editForm(pages[i])
      - i++;
    br
    button(id="submit")  Submit


block scripts
  script(src="/javascript/editHome.js")
