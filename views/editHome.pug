extends layout

include mixins/_editForm.pug

block content
  .editForms
    -let i = 0;
    while i < 3
      .formSection
        +editForm(pages[i])
      - i++;
    button(id="submit")  Submit 
  

block scripts
  script(src="/javascript/editHome.js")